// Copilot:
// Create a minimal Node.js package.json for an Express API.
// Dependencies: express, pg, jsonwebtoken, express-rate-limit, helmet.
// Script: "start": "node server.js".
// No devDependencies. Keep it simple.
{
  "name": "backend.asinu",
  "version": "1.0.0",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "pg": "^8.11.3",
    "jsonwebtoken": "^9.0.2",
    "express-rate-limit": "^7.0.0",
    "helmet": "^7.0.0"
  }
}
